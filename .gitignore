# Downloaded sources for the data pipeline. Never committed; fetch.sh gets them.
build/sources/

# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/

# Staged runtime data root. tools/stage-data.sh rebuilds it from build/ and data/.
build/LinguaKeyData/

# SwiftPM
.build/
.swiftpm/
Package.resolved

# Free personal team identifiers are per developer and must not be committed.
LinguaKeyApp/Local.xcconfig
